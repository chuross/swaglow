get:
  tags: ['Items']
  parameters:
    - name: q
      in: query
      required: true
      type: string
    - $ref: '#/parameters/offset'
    - $ref: '#/parameters/limit'
  responses:
    200:
      description: ''
      schema:
        type: array
        items:
          $ref: '#/definitions/Item'
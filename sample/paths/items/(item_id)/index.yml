get:
  tags: ['Items']
  parameters:
    - name: item_id
      in: path
      required: true
      type: string
  responses:
    200:
      description: ''
      schema:
        $ref: '#/definitions/Item'